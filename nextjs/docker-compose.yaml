version: '3'

services:
  app:
    build: .
    command: npm run dev
    ports:
      - 3001:3000
    volumes:
      - .:/home/node/app:cached
    extra_hosts:
      - "host.docker.internal:192.168.65.0"